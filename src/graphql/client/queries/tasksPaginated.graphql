query TasksPaginated($limit: Int!, $offset: Int!, $filter: TaskFilterInput) {
  tasksPaginated(filter: $filter, limit: $limit, offset: $offset) {
    totalCount
    items {
      ...TaskFullFields
    }
  }
}
